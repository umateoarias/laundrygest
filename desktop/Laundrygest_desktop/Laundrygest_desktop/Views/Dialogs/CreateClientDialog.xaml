<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Laundrygest_desktop.Views"
        xmlns:ViewModel="clr-namespace:Laundrygest_desktop.ViewModel" x:Class="Laundrygest_desktop.Views.CreateClientDialog"
        mc:Ignorable="d"
        x:Name="CreateClientWindow"
        Title="CreateClientDialog" Height="550" Width="650" Background="#F4F4F4"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <!--<Window.DataContext>
        <ViewModel:CreateClientDialogViewModel/>
    </Window.DataContext>-->
    <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Margin="20" Background="White">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Crear client" FontSize="26" FontWeight="Bold" Margin="0,0,0,20" />

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
                    <TextBlock Text="Nom" />
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding FirstNameTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
                    <TextBlock Text="Telèfon" />
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding TelephoneTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="0" Margin="5">
                    <TextBlock Text="Codi postal" />
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding PostalCodeTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Row="3" Grid.Column="0" Margin="5">
                    <TextBlock Text="Localitat" />
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding LocalityTextBox}"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
                    <TextBlock Text="Cognom" />
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding LastNameTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                    <TextBlock Text="Adreça" />
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding AddressTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="1" Margin="5">
                    <TextBlock Text="Email" />
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding EmailTextBox}"/>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,20,0,0">
                <CheckBox x:Name="checkBoxEmpresa" Content="És empresa?" VerticalAlignment="Center" Margin="0,0,10,0" IsChecked="{Binding CheckBoxState}"/>
                <TextBox Width="200"  IsEnabled="{Binding IsChecked, ElementName=checkBoxEmpresa}" Style="{StaticResource ModernTextBox}" Text="{Binding NifTextBox}"/>
            </StackPanel>

            <DockPanel Grid.Row="3" Margin="0,30,0,0" LastChildFill="False">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <Button Content="{Binding ConfirmButtonContent}" Style="{StaticResource PrimaryButton}" Width="100" Margin="5,0" Command="{Binding CreateClientCommand}" CommandParameter="{Binding ElementName=CreateClientWindow}"/>
                    <Button Content="Cancel·lar" Style="{StaticResource SecondaryButton}" Command="{Binding CloseWindowCommand}" CommandParameter="{Binding ElementName=CreateClientWindow}" Width="100" Margin="5,0"/>
                </StackPanel>
            </DockPanel>
        </Grid>
    </Border>
</Window>